// src/utils/smartScheduler.js
export function getSmartRecommendations() {
  return [
    "🔍 Shift A needs more volunteers",
    "⚠️ Skill mismatch in Shift C",
    "📌 Mike is overloaded. Reassign tasks",
  ];
}
